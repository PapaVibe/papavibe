{
  "name": "@papavibe/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc --project tsconfig.json",
    "check": "tsc --noEmit --project tsconfig.json",
    "test": "node --import tsx --test src/review-engine.test.ts src/api-boundary.test.ts src/contract.test.ts"
  },
  "dependencies": {
    "fastify": "^5.2.1"
  }
}
